<?php

declare(strict_types=1);

// empty config file because we have to have a file for git to keep the config/ directory, and we can't have a
//.gitignore file as the config reader in the Application class will choke on it
return [];
